[pytest]
testpaths = tests
filterwarnings =
    # fpdf2 deprecated the `ln=` cell parameter; shieldlab/report/report.py still
    # uses it. Surface it once rather than 60+ times per run.
    once::DeprecationWarning
